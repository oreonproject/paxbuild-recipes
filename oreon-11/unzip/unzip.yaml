name: unzip
version: "6.0"
description: "Info-ZIP unzip - extraction utility for .zip files"
author: "Info-ZIP"
license: "BSD-like"
homepage: "http://www.info-zip.org/UnZip.html"
repository: ""
source_url: "https://downloads.sourceforge.net/infozip/unzip60.tar.gz"
keywords: [unzip, extract, archive]
categories: [system, utilities]

dependencies:
  build_dependencies:
    - {name: "gcc", version_constraint: ">=11", optional: false}
    - {name: "make", version_constraint: ">=4.3", optional: false}
  runtime_dependencies:
    - {name: "glibc", version_constraint: null, optional: false}

  optional_dependencies: []
  conflicts: []

build:
  build_system: Make
  build_commands:
    - "make -f unix/Makefile generic"
  build_dependencies:
    - "gcc"
    - "make"
  build_flags: []
  environment:
    CFLAGS: "-O2 -pipe"
  working_directory: null
  target_architectures: [X86_64v1, X86_64v3, Aarch64]
  cross_compiler_prefix: null
  target_sysroot: null

install:
  install_method: RunCommands
  install_commands:
    - "make -f unix/Makefile prefix=$DESTDIR/usr MANDIR=$DESTDIR/usr/share/man/man1 install"
  install_directories: ["/usr/bin"]
  install_files: []
  post_install_commands: []

files:
  include_patterns: ["**/*.c", "**/*.h", "unix/Makefile"]
  exclude_patterns: ["**/*.o"]
  binary_files: ["usr/bin/unzip", "usr/bin/funzip", "usr/bin/unzipsfx", "usr/bin/zipinfo"]
  config_files: []
  documentation_files: []
  license_files: ["LICENSE"]

scripts:
  pre_install: "echo 'Installing unzip...'"
  post_install: "echo 'unzip installed'"
  pre_uninstall: null
  post_uninstall: null
  pre_upgrade: null
  post_upgrade: null

metadata:
  maintainer: "Oreon Project Team <packaging@oreonproject.org>"
  section: "utilities"
  priority: "standard"
  provides: ["unzip"]
  conflicts: []
